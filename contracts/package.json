{
  "name": "ogsecura-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for 0gSecura authentication and security features",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network og-galileo",
    "verify": "hardhat verify --network og-galileo",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "ethers": "^6.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
